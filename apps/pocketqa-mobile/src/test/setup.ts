// @testing-library/react-native ships its jest matchers by default from v12.4,
// and removed the separate "extend-expect" entry point in v14. Importing it now
// fails to resolve; nothing needs to replace it.
export {};
