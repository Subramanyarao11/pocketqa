<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:usesCleartextTraffic="false">

        <!--
          Fixture deep-link: pocketqa-demo://fixture/<name>
          Used by PocketQA's ReplayExecutor.launchTarget() to reset the
          in-memory shop state before a replay. Exported so the harness can
          trigger it, but the intent-filter's data schema limits it to us.
        -->
        <activity
            android:name=".ShopActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="pocketqa-demo" android:host="fixture" />
            </intent-filter>
        </activity>
    </application>
</manifest>
